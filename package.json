{
  "name": "pb-extensions",
  "version": "1.0.0",
  "description": "A paperback extension repository",
  "main": "index.js",
  "scripts": {
    "build": "tsc && copy manifest.json dist\\",
    "build-online": "tsc && cp manifest.json dist/",
    "watch": "pb watch",
    "test": "npx paperback test",
    "bundle": "npx paperback bundle --folder=src/sources"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0",
    "paperback-extensions-common": "^4.3.5"
  },
  "devDependencies": {
    "@types/cheerio": "^0.22.35",
    "@types/node": "^22.13.5",
    "paperback-cli": "^0.8.0-alpha.4",
    "typescript": "^5.7.3"
  }
}
